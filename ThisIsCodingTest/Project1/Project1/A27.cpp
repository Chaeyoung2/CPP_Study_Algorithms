// chap15. 이진 탐색. 정렬된 배열에서 특정 수의 개수 구하기.
// 다시 풀어~

#include <iostream>
#include <vector>
#include <algorithm>
using namespace std;

int n, x;
vector<int> arr;

int main() {
	cin >> n >> x;
	for (int i = 0; i < n; i++) {
		int val;
		cin >> val;
		arr.push_back(val);
	}

}
